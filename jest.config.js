module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  verbose: true,
  moduleFileExtensions: ['ts', 'js', 'json'],
  testMatch: ['<rootDir>/Algorithm/二分查找-查找第一个大于等于给定值的元素/test.spec.ts'],
};
